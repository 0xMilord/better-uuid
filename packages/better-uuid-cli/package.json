{
  "name": "better-uuid-cli",
  "version": "0.0.0",
  "description": "CLI for better-uuid — generate, parse, migrate, bench",
  "type": "module",
  "bin": {
    "better-uuid": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "echo 'skipped: depends on better-uuid which must be built first'",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "better-uuid": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT"
}
